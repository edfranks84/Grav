<section class="latest-news">
	<div class="latest-news-wrap">
		{% for post in taxonomy.findTaxonomy({'category':'post'}).children.order('date', 'desc').slice(0, 4) %}
			<div class="latest-news__item">
				<a href={{ post.url }}> {{post.title}}</a>
			</div>
		{% endfor %}
	</div>
</section>